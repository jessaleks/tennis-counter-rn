import {tailwind} from "../tailwind";

const backgroundColor = tailwind('bg-gray-900')
const headerColor = tailwind("bg-gray-700")
const activeTextColor = tailwind('text-gray-200')

export {backgroundColor, headerColor, activeTextColor};

